import React, { useEffect, useMemo, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { createClient } from '@supabase/supabase-js';
import { Home, Plus, Brush, BarChart3, Wallet, Building2, Wrench, LogOut, Download, Phone, Shield, Users, Database, TrendingUp, Edit3, X } from 'lucide-react';
import './style.css';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseAnonKey = import.meta.env.VITE_SUPABASE_ANON_KEY;
const supabase = supabaseUrl && supabaseAnonKey ? createClient(supabaseUrl, supabaseAnonKey) : null;

const tabs = [
  { id: 'dashboard', label: 'Home', icon: Home },
  { id: 'rooms', label: 'Rooms', icon: Building2 },
  { id: 'add', label: 'Add', icon: Plus },
  { id: 'reports', label: 'Reports', icon: BarChart3 },
  { id: 'cleaning', label: 'Cleaning', icon: Brush },
  { id: 'repairs', label: 'Repairs', icon: Wrench },
  { id: 'finance', label: 'Finance', icon: Wallet },
  { id: 'users', label: 'Users', icon: Users },
];

const roles = ['Boss','Admin','Cleaner','Maintenance'];
const sources = ['Airbnb','Booking.com','Walk-in','Referral','Other'];
const todayISO = () => new Date().toISOString().slice(0,10);
const money = n => '₦' + Number(n || 0).toLocaleString();
const monthKey = (d = new Date()) => typeof d === 'string' ? d.slice(0,7) : d.toISOString().slice(0,7);
function formatDate(dateStr){ if(!dateStr) return '-'; const d = new Date(dateStr+'T00:00:00'); if(Number.isNaN(d.getTime())) return dateStr; const weekday=d.toLocaleDateString('en-US',{weekday:'short'}); const day=String(d.getDate()).padStart(2,'0'); const month=d.toLocaleDateString('en-US',{month:'short'}); const year=d.getFullYear(); return `${weekday}, ${day}-${month}-${year}`; }
function monthShort(month){ if(!month) return ''; const d=new Date(month+'-01T00:00:00'); return d.toLocaleDateString('en-US',{month:'short'}); }
function monthDashboardTitle(month){ const label=monthShort(month)||month; return `${label} Occupancy Dashboard`; }
function daysInMonth(month){ const [y,m]=month.split('-').map(Number); return new Date(y,m,0).getDate(); }
function overlapDays(start,end,month){ if(!start||!end) return 0; const [y,m]=month.split('-').map(Number); const ms=new Date(y,m-1,1); const me=new Date(y,m,1); const s=new Date(start+'T00:00:00'); const e=new Date(end+'T00:00:00'); const from=new Date(Math.max(s,ms)); const to=new Date(Math.min(e,me)); return Math.max(0, Math.round((to-from)/86400000)); }
function nights(b){ if(!b.check_in||!b.check_out) return 0; return Math.max(1,Math.round((new Date(b.check_out)-new Date(b.check_in))/86400000)); }
function downloadFile(name, content, type='application/json'){ const blob=new Blob([content],{type}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=name; a.click(); URL.revokeObjectURL(url); }
function toCSV(rows){ if(!rows.length) return ''; const keys=Object.keys(rows[0]); return [keys.join(','),...rows.map(r=>keys.map(k=>`"${String(r[k]??'').replace(/"/g,'""')}"`).join(','))].join('\n'); }
function whatsappUrl(phone){ const digits=String(phone||'').replace(/\D/g,''); return digits ? `https://wa.me/${digits}` : ''; }
function isoAdd(dateStr, days){ const d=new Date(dateStr+'T00:00:00'); d.setDate(d.getDate()+days); return d.toISOString().slice(0,10); }
function dateOptions(days=365){ const start=todayISO(); return Array.from({length:days},(_,i)=>isoAdd(start,i)); }
function overlaps(aStart,aEnd,bStart,bEnd){ return !!(aStart&&aEnd&&bStart&&bEnd&&aStart<bEnd&&aEnd>bStart); }
function dateInRange(date,start,end){ return !!(date&&start&&end&&date>=start&&date<end); }
function unitPriceFrom(total, checkIn, checkOut){ const q=nights({check_in:checkIn,check_out:checkOut}); return q>0 ? Math.round(Number(total||0)/q) : 0; }
function formatMonthYear(month){ const d=new Date(month+'-01T00:00:00'); return d.toLocaleDateString('en-US',{month:'short',year:'numeric'}); }

function Card({children, className=''}){ return <div className={'card '+className}>{children}</div>; }
function Button({children,onClick,kind='primary',type='button',disabled=false}){ return <button type={type} disabled={disabled} onClick={onClick} className={'btn '+kind}>{children}</button>; }
function Stat({title,value}){ return <Card><div className="muted small">{title}</div><div className="stat">{value}</div></Card>; }
function Section({title, icon}){ return <h2 className="section">{icon}{title}</h2>; }

function App(){
  const [session,setSession]=useState(null); const [authMode,setAuthMode]=useState('login');
  const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [fullName,setFullName]=useState('');
  const [profile,setProfile]=useState(null); const [active,setActive]=useState('dashboard'); const [loading,setLoading]=useState(false);
  const [rooms,setRooms]=useState([]); const [bookings,setBookings]=useState([]); const [tasks,setTasks]=useState([]); const [repairs,setRepairs]=useState([]); const [expenses,setExpenses]=useState([]); const [profiles,setProfiles]=useState([]);
  const [selectedMonth,setSelectedMonth]=useState(monthKey());
  const [roomName,setRoomName]=useState(''); const [roomNote,setRoomNote]=useState('');
  const emptyBooking=()=>({room_id:'',guest:'',guest_phone:'',source:'Airbnb',check_in:todayISO(),check_out:'',qty:0,unit_price:'',total:'',paid:''});
  const [booking,setBooking]=useState(emptyBooking());
  const [paymentStatus,setPaymentStatus]=useState('Paid');
  const [editingBooking,setEditingBooking]=useState(null);
  const [editPaymentStatus,setEditPaymentStatus]=useState('Paid');
  const [repair,setRepair]=useState({room_id:'',issue:''}); const [expense,setExpense]=useState({category:'Cleaning',amount:'',expense_date:todayISO(),note:''});

  useEffect(()=>{ if(!supabase) return; supabase.auth.getSession().then(({data})=>setSession(data.session)); const {data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s)); return ()=>subscription.unsubscribe(); },[]);
  useEffect(()=>{ if(session?.user) { loadAll(); } },[session]);

  async function sign(){ if(!supabase) return alert('Supabase env variables are missing.'); setLoading(true); try{
    if(authMode==='signup'){
      const {data,error}=await supabase.auth.signUp({email,password}); if(error) throw error;
      if(data.user) await supabase.from('profiles').upsert({id:data.user.id,email,full_name:fullName||email,role:'Admin'});
      alert('Account created. If email confirmation is enabled, check your mailbox.');
    } else { const {error}=await supabase.auth.signInWithPassword({email,password}); if(error) throw error; }
  } catch(e){ alert(e.message); } finally{ setLoading(false); } }
  async function logout(){ await supabase.auth.signOut(); setSession(null); }

  async function loadAll(){ setLoading(true); try{
    const uid=session.user.id;
    let {data:p}=await supabase.from('profiles').select('*').eq('id',uid).maybeSingle();
    if(!p){ const {data:existing}=await supabase.from('profiles').select('id').limit(1); const role=(existing||[]).length===0?'Boss':'Admin'; const up={id:uid,email:session.user.email,full_name:session.user.email,role}; await supabase.from('profiles').upsert(up); p=up; }
    setProfile(p);
    const [r,b,t,rep,exp,prof]=await Promise.all([
      supabase.from('rooms').select('*').order('created_at',{ascending:true}),
      supabase.from('bookings').select('*').order('check_in',{ascending:false}),
      supabase.from('cleaning_tasks').select('*').order('task_date',{ascending:true}),
      supabase.from('repairs').select('*').order('created_at',{ascending:false}),
      supabase.from('expenses').select('*').order('expense_date',{ascending:false}),
      supabase.from('profiles').select('*').order('created_at',{ascending:true}),
    ]);
    setRooms(r.data||[]); setBookings(b.data||[]); setTasks(t.data||[]); setRepairs(rep.data||[]); setExpenses(exp.data||[]); setProfiles(prof.data||[]);
  } finally{ setLoading(false); } }

  const canFinance = ['Boss','Admin'].includes(profile?.role); const canManage = ['Boss','Admin'].includes(profile?.role); const canUsers = profile?.role==='Boss';
  const currentBookings = useMemo(()=>bookings.filter(b=>b.check_in<=todayISO() && b.check_out>todayISO()),[bookings]);
  const monthly = useMemo(()=>rooms.map(room=>{ const rb=bookings.filter(b=>b.room_id===room.id); const totalDays=daysInMonth(selectedMonth); const occupied=rb.reduce((s,b)=>s+overlapDays(b.check_in,b.check_out,selectedMonth),0); const income=rb.reduce((s,b)=>s+(nights(b)?Number(b.paid||0)*overlapDays(b.check_in,b.check_out,selectedMonth)/nights(b):0),0); return {room_id:room.id,room:room.name,occupied,totalDays,occupancy: totalDays?Math.round(occupied/totalDays*100):0,income:Math.round(income)}; }),[rooms,bookings,selectedMonth]);
  const trend = useMemo(()=>{ const arr=[]; const d=new Date(selectedMonth+'-01T00:00:00'); for(let i=5;i>=0;i--){ const x=new Date(d.getFullYear(),d.getMonth()-i,1); const m=monthKey(x); const revenue=bookings.reduce((s,b)=>s+(nights(b)?Number(b.paid||0)*overlapDays(b.check_in,b.check_out,m)/nights(b):0),0); arr.push({month:m,revenue:Math.round(revenue)}); } return arr; },[bookings,selectedMonth]);
  const sourceRows = useMemo(()=>sources.map(src=>({source:src,revenue:bookings.filter(b=>b.source===src).reduce((s,b)=>s+Number(b.paid||0),0)})).filter(x=>x.revenue>0),[bookings]);
  const stats = { occupied: currentBookings.length, available: Math.max(0,rooms.length-currentBookings.length), pendingCleaning: tasks.filter(t=>t.status!=='Done').length, unpaid: bookings.reduce((s,b)=>s+Math.max(0,Number(b.total||0)-Number(b.paid||0)),0), received: bookings.reduce((s,b)=>s+Number(b.paid||0),0), expense: expenses.reduce((s,e)=>s+Number(e.amount||0),0) };
  const selectedRoomBookings = useMemo(()=>bookings.filter(b=>b.room_id===booking.room_id).sort((a,b)=>String(a.check_in).localeCompare(String(b.check_in))),[bookings,booking.room_id]);
  const editRoomBookings = useMemo(()=>bookings.filter(b=>editingBooking && b.room_id===editingBooking.room_id && b.id!==editingBooking.id).sort((a,b)=>String(a.check_in).localeCompare(String(b.check_in))),[bookings,editingBooking]);
  const checkInOptions = useMemo(()=>dateOptions(365).filter(d=>!selectedRoomBookings.some(b=>dateInRange(d,b.check_in,b.check_out))),[selectedRoomBookings]);
  const editCheckInOptions = useMemo(()=>dateOptions(365).filter(d=>!editRoomBookings.some(b=>dateInRange(d,b.check_in,b.check_out))),[editRoomBookings]);
  const checkOutOptions = useMemo(()=>{ if(!booking.check_in) return []; const next=selectedRoomBookings.find(b=>b.check_in>booking.check_in); const limit=next?.check_in || isoAdd(booking.check_in,60); const arr=[]; for(let d=isoAdd(booking.check_in,1); d<=limit; d=isoAdd(d,1)) arr.push(d); return arr; },[booking.check_in,selectedRoomBookings]);
  const editCheckOutOptions = useMemo(()=>{ if(!editingBooking?.check_in) return []; const next=editRoomBookings.find(b=>b.check_in>editingBooking.check_in); const limit=next?.check_in || isoAdd(editingBooking.check_in,60); const arr=[]; for(let d=isoAdd(editingBooking.check_in,1); d<=limit; d=isoAdd(d,1)) arr.push(d); return arr; },[editingBooking?.check_in,editRoomBookings]);
  const bookingConflict = useMemo(()=>selectedRoomBookings.find(b=>overlaps(booking.check_in,booking.check_out,b.check_in,b.check_out)),[booking,selectedRoomBookings]);
  const editConflict = useMemo(()=>editingBooking ? editRoomBookings.find(b=>overlaps(editingBooking.check_in,editingBooking.check_out,b.check_in,b.check_out)) : null,[editingBooking,editRoomBookings]);
  const financeSummary = useMemo(()=>{ const y=selectedMonth.slice(0,4); const monthReceived=bookings.reduce((sum,b)=>sum+(nights(b)?Number(b.paid||0)*overlapDays(b.check_in,b.check_out,selectedMonth)/nights(b):0),0); const yearReceived=bookings.reduce((sum,b)=>String(b.check_in||'').slice(0,4)===y?sum+Number(b.paid||0):sum,0); const monthExpenses=expenses.filter(e=>String(e.expense_date||'').slice(0,7)===selectedMonth); const yearExpenses=expenses.filter(e=>String(e.expense_date||'').slice(0,4)===y); const cat=(list,name)=>list.reduce((sum,e)=>String(e.category||'').toLowerCase().includes(name)?sum+Number(e.amount||0):sum,0); const monthCleaningTasks=tasks.filter(t=>String(t.task_date||'').slice(0,7)===selectedMonth).reduce((sum,t)=>sum+Number(t.cost||0),0); const yearCleaningTasks=tasks.filter(t=>String(t.task_date||'').slice(0,4)===y).reduce((sum,t)=>sum+Number(t.cost||0),0); const daily=cat(monthExpenses,'daily')+cat(monthExpenses,'suppl'); const cleaning=cat(monthExpenses,'clean')+monthCleaningTasks; const repairCost=cat(monthExpenses,'repair'); const yDaily=cat(yearExpenses,'daily')+cat(yearExpenses,'suppl'); const yCleaning=cat(yearExpenses,'clean')+yearCleaningTasks; const yRepair=cat(yearExpenses,'repair'); const soldNights=monthly.reduce((s,r)=>s+r.occupied,0); const availableNights=rooms.length*daysInMonth(selectedMonth); const occ=availableNights?Math.round(soldNights/availableNights*100):0; const adr=soldNights?Math.round(monthReceived/soldNights):0; const expensesTotal=daily+cleaning+repairCost; return {year:y,monthReceived:Math.round(monthReceived),yearReceived:Math.round(yearReceived),daily,cleaning,repairCost,net:Math.round(monthReceived-expensesTotal),yDaily,yCleaning,yRepair,yNet:Math.round(yearReceived-yDaily-yCleaning-yRepair),soldNights,availableNights,occ,adr,expenseRatio:monthReceived?Math.round(expensesTotal/monthReceived*100):0}; },[selectedMonth,bookings,expenses,tasks,monthly,rooms]);

  function smartBooking(patch){ setBooking(prev=>{ const next={...prev,...patch}; const q=nights({check_in:next.check_in,check_out:next.check_out}); next.qty=q; if('unit_price' in patch){ const u=Number(patch.unit_price||0); if(q>0&&u>0) next.total=String(Math.round(q*u)); } if('total' in patch){ const total=Number(patch.total||0); if(q>0&&total>0) next.unit_price=String(Math.round(total/q)); } if(('check_in' in patch)||('check_out' in patch)){ const u=Number(next.unit_price||0); const total=Number(next.total||0); if(q>0&&u>0) next.total=String(Math.round(q*u)); else if(q>0&&total>0) next.unit_price=String(Math.round(total/q)); } if(paymentStatus==='Paid') next.paid=next.total||'0'; if(paymentStatus==='Unpaid') next.paid='0'; return next; }); }
  function smartEditBooking(patch){ setEditingBooking(prev=>{ const next={...prev,...patch}; const q=nights({check_in:next.check_in,check_out:next.check_out}); next.qty=q; if('unit_price' in patch){ const u=Number(patch.unit_price||0); if(q>0&&u>0) next.total=String(Math.round(q*u)); } if('total' in patch){ const total=Number(patch.total||0); if(q>0&&total>0) next.unit_price=String(Math.round(total/q)); } if(('check_in' in patch)||('check_out' in patch)){ const u=Number(next.unit_price||0); const total=Number(next.total||0); if(q>0&&u>0) next.total=String(Math.round(q*u)); else if(q>0&&total>0) next.unit_price=String(Math.round(total/q)); } if(editPaymentStatus==='Paid') next.paid=next.total||'0'; if(editPaymentStatus==='Unpaid') next.paid='0'; return next; }); }
  async function addRoom(){ if(!roomName.trim()) return; await supabase.from('rooms').insert({name:roomName.trim(),note:roomNote,status:'Available'}); setRoomName(''); setRoomNote(''); loadAll(); }
  async function addBooking(){ const room=rooms.find(r=>r.id===booking.room_id); if(!room||!booking.guest||!booking.check_out) return alert('Room, guest and check-out are required.'); if(bookingConflict) return alert(`This room is already booked from ${formatDate(bookingConflict.check_in)} to ${formatDate(bookingConflict.check_out)}.`); const payload={room_id:booking.room_id,room_name:room.name,guest:booking.guest,guest_phone:booking.guest_phone,source:booking.source,check_in:booking.check_in,check_out:booking.check_out,total:Number(booking.total||0),paid:Number(booking.paid||0)}; const {error}=await supabase.from('bookings').insert(payload); if(error) return alert(error.message); await supabase.from('cleaning_tasks').insert({room_id:room.id,room_name:room.name,task_type:'Cleaning + Bedding',task_date:booking.check_out,assignee:'Cleaner',status:'Pending',cost:8000}); setBooking(emptyBooking()); setPaymentStatus('Paid'); loadAll(); }
  function startEditBooking(b){ const total=Number(b.total||0); const paid=Number(b.paid||0); const q=nights(b); setEditPaymentStatus(total>0 && paid>=total ? 'Paid' : paid===0 ? 'Unpaid' : 'Partial'); setEditingBooking({...b,qty:q,unit_price:String(unitPriceFrom(total,b.check_in,b.check_out)||''),total:String(b.total||''),paid:String(b.paid||'')}); setActive('add'); window.scrollTo({top:0,behavior:'smooth'}); }
  function cancelEditBooking(){ setEditingBooking(null); }
  async function saveEditBooking(){ if(!editingBooking) return; const room=rooms.find(r=>r.id===editingBooking.room_id); if(!room||!editingBooking.guest||!editingBooking.check_out) return alert('Room, guest and check-out are required.'); if(editConflict) return alert(`This room is already booked from ${formatDate(editConflict.check_in)} to ${formatDate(editConflict.check_out)}.`); const payload={room_id:editingBooking.room_id,room_name:room.name,guest:editingBooking.guest,guest_phone:editingBooking.guest_phone||'',source:editingBooking.source||'Airbnb',check_in:editingBooking.check_in,check_out:editingBooking.check_out,total:Number(editingBooking.total||0),paid:Number(editingBooking.paid||0)}; const {error}=await supabase.from('bookings').update(payload).eq('id',editingBooking.id); if(error) return alert(error.message); setEditingBooking(null); loadAll(); }
  async function addRepair(){ const room=rooms.find(r=>r.id===repair.room_id); if(!repair.issue) return; await supabase.from('repairs').insert({room_id:repair.room_id||null,room_name:room?.name||'',issue:repair.issue,status:'Pending'}); setRepair({room_id:'',issue:''}); loadAll(); }
  async function addExpense(){ if(!expense.category||!expense.amount) return; await supabase.from('expenses').insert({...expense,amount:Number(expense.amount)}); setExpense({category:'Cleaning',amount:'',expense_date:todayISO(),note:''}); loadAll(); }
  async function toggleTask(id,status){ await supabase.from('cleaning_tasks').update({status:status==='Done'?'Pending':'Done'}).eq('id',id); loadAll(); }
  async function solveRepair(id){ await supabase.from('repairs').update({status:'Solved'}).eq('id',id); loadAll(); }
  async function updateRole(id,role){ await supabase.from('profiles').update({role}).eq('id',id); loadAll(); }
  const backupPayload=()=>({exported_at:new Date().toISOString(),rooms,bookings,cleaning_tasks:tasks,repairs,expenses,profiles});
  async function saveBackup(){ const payload=backupPayload(); await supabase.from('backups').insert({payload,created_by:session.user.id}); downloadFile(`gra-stay-backup-${todayISO()}.json`,JSON.stringify(payload,null,2)); }
  function exportMonthlyCSV(){ downloadFile(`monthly-report-${selectedMonth}.csv`,toCSV(monthly),'text/csv'); }
  function exportBookingsCSV(){ downloadFile(`bookings-${todayISO()}.csv`,toCSV(bookings),'text/csv'); }

  if(!supabase) return <div className="center"><Card><h1>Missing Supabase settings</h1><p className="muted">Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in Vercel Environment Variables.</p></Card></div>;
  if(!session) return <div className="center"><Card className="login"><h1>GRA Stay Manager V6</h1><p className="muted">Secure mobile room management for Airbnb / Booking operations.</p>{authMode==='signup'&&<input placeholder="Full name" value={fullName} onChange={e=>setFullName(e.target.value)}/>}<input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><Button onClick={sign} disabled={loading}>{authMode==='login'?'Login':'Create account'}</Button><button className="link" onClick={()=>setAuthMode(authMode==='login'?'signup':'login')}>{authMode==='login'?'Create new account':'Back to login'}</button></Card></div>;

  return <div className="app"><header><div className="header-title"><h1>GRA Stay Manager</h1><p>{profile?.full_name||profile?.email} · {profile?.role}</p></div><div className="head-actions"><Button kind="light" onClick={loadAll}><Database size={16}/> Sync</Button><Button kind="light" onClick={logout}><LogOut size={16}/></Button></div></header><main>
    {active==='dashboard'&&<><Section title="Room status" icon={<Building2/>}/>{rooms.map(r=><RoomCard key={r.id} room={r} booking={currentBookings.find(b=>b.room_id===r.id)} canFinance={canFinance} canManage={canManage} onEdit={startEditBooking}/>) }<div className="grid"><Stat title="Occupied" value={stats.occupied}/><Stat title="Available" value={stats.available}/><Stat title="Pending Cleaning" value={stats.pendingCleaning}/><Stat title="Unpaid" value={canFinance?money(stats.unpaid):'Hidden'}/></div><Section title={monthDashboardTitle(selectedMonth)} icon={<BarChart3/>}/><OccupancyDashboard rows={monthly} title={monthDashboardTitle(selectedMonth)}/><Section title="Revenue trend" icon={<TrendingUp/>}/>{canFinance?<LineChart rows={trend}/>:<p className="muted">Finance data hidden for this role.</p>}</>}
    {active==='add'&&<div className="stack">{editingBooking&&<><Section title="Edit booking" icon={<Edit3/>}/><Card><select value={editingBooking.room_id||''} onChange={e=>smartEditBooking({room_id:e.target.value,check_in:todayISO(),check_out:''})}><option value="">Select room</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select>{editRoomBookings.length>0&&<div className="notice"><b>Already booked:</b>{editRoomBookings.map(b=><p key={b.id}>{formatDate(b.check_in)} → {formatDate(b.check_out)} · {b.guest}</p>)}</div>}<input placeholder="Guest name" value={editingBooking.guest||''} onChange={e=>smartEditBooking({guest:e.target.value})}/><input placeholder="WhatsApp / phone" value={editingBooking.guest_phone||''} onChange={e=>smartEditBooking({guest_phone:e.target.value})}/><select value={editingBooking.source||'Airbnb'} onChange={e=>smartEditBooking({source:e.target.value})}>{sources.map(s=><option key={s}>{s}</option>)}</select><div className="two"><select value={editingBooking.check_in||''} onChange={e=>smartEditBooking({check_in:e.target.value,check_out:''})}><option value="">Check-in</option>{editCheckInOptions.map(d=><option key={d} value={d}>{formatDate(d)}</option>)}</select><select value={editingBooking.check_out||''} onChange={e=>smartEditBooking({check_out:e.target.value})} disabled={!editingBooking.check_in}><option value="">Check-out</option>{editCheckOutOptions.map(d=><option key={d} value={d}>{formatDate(d)}</option>)}</select></div>{editConflict&&<p className="error">Date conflict: {formatDate(editConflict.check_in)} → {formatDate(editConflict.check_out)}</p>}<div className="grid mini"><Stat title="Qty / Nights" value={nights({check_in:editingBooking.check_in,check_out:editingBooking.check_out})}/><Stat title="Unit Price" value={money(editingBooking.unit_price||unitPriceFrom(editingBooking.total,editingBooking.check_in,editingBooking.check_out))}/></div>{canFinance&&<><select value={editPaymentStatus} onChange={e=>{ const status=e.target.value; setEditPaymentStatus(status); smartEditBooking({paid: status==='Paid' ? String(editingBooking.total||0) : status==='Unpaid' ? '0' : String(editingBooking.paid||'')}); }}><option>Paid</option><option>Partial</option><option>Unpaid</option></select><div className="two"><input placeholder="Unit price / night" value={editingBooking.unit_price||''} onChange={e=>smartEditBooking({unit_price:e.target.value})}/><input placeholder="Total amount" value={editingBooking.total||''} onChange={e=>smartEditBooking({total:e.target.value})}/></div><div className="two"><input placeholder="Paid amount" value={editingBooking.paid||''} onChange={e=>{ setEditPaymentStatus(Number(e.target.value||0)>=Number(editingBooking.total||0)?'Paid':'Partial'); smartEditBooking({paid:e.target.value}); }}/><input readOnly value={'Unpaid: '+money(Math.max(0,Number(editingBooking.total||0)-Number(editingBooking.paid||0)))}/></div><p className="muted small">You can enter Unit Price to calculate Total, or enter Total to calculate Unit Price automatically.</p></>}<div className="actions"><Button disabled={!!editConflict} onClick={saveEditBooking}><Edit3 size={16}/> Save Changes</Button><Button kind="light" onClick={cancelEditBooking}><X size={16}/> Cancel</Button></div></Card></>}<Section title="Add room" icon={<Building2/>}/>{canManage?<Card><input placeholder="Room name, e.g. Flat 13" value={roomName} onChange={e=>setRoomName(e.target.value)}/><input placeholder="Note" value={roomNote} onChange={e=>setRoomNote(e.target.value)}/><Button onClick={addRoom}>Add Room</Button></Card>:<p className="muted">No permission.</p>}<Section title="Add booking" icon={<Plus/>}/><Card><select value={booking.room_id} onChange={e=>smartBooking({room_id:e.target.value,check_in:todayISO(),check_out:''})}><option value="">Select room</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select>{selectedRoomBookings.length>0&&<div className="notice"><b>Already booked:</b>{selectedRoomBookings.map(b=><p key={b.id}>{formatDate(b.check_in)} → {formatDate(b.check_out)} · {b.guest}</p>)}</div>}<input placeholder="Guest name" value={booking.guest} onChange={e=>smartBooking({guest:e.target.value})}/><input placeholder="WhatsApp / phone, e.g. 2348012345678" value={booking.guest_phone} onChange={e=>smartBooking({guest_phone:e.target.value})}/><select value={booking.source} onChange={e=>smartBooking({source:e.target.value})}>{sources.map(s=><option key={s}>{s}</option>)}</select><div className="two"><select value={booking.check_in} onChange={e=>smartBooking({check_in:e.target.value,check_out:''})} disabled={!booking.room_id}><option value="">Check-in</option>{checkInOptions.map(d=><option key={d} value={d}>{formatDate(d)}</option>)}</select><select value={booking.check_out} onChange={e=>smartBooking({check_out:e.target.value})} disabled={!booking.check_in}><option value="">Check-out</option>{checkOutOptions.map(d=><option key={d} value={d}>{formatDate(d)}</option>)}</select></div>{bookingConflict&&<p className="error">Date conflict: {formatDate(bookingConflict.check_in)} → {formatDate(bookingConflict.check_out)}</p>}<div className="grid mini"><Stat title="Qty / Nights" value={nights({check_in:booking.check_in,check_out:booking.check_out})}/><Stat title="Unit Price" value={money(booking.unit_price||unitPriceFrom(booking.total,booking.check_in,booking.check_out))}/></div>{canFinance&&<><select value={paymentStatus} onChange={e=>{ const status=e.target.value; setPaymentStatus(status); smartBooking({paid: status==='Paid' ? String(booking.total||0) : status==='Unpaid' ? '0' : String(booking.paid||'')}); }}><option>Paid</option><option>Partial</option><option>Unpaid</option></select><div className="two"><input placeholder="Unit price / night" value={booking.unit_price} onChange={e=>smartBooking({unit_price:e.target.value})}/><input placeholder="Total amount" value={booking.total} onChange={e=>smartBooking({total:e.target.value})}/></div><div className="two"><input placeholder="Paid amount" value={booking.paid} onChange={e=>{ setPaymentStatus(Number(e.target.value||0)>=Number(booking.total||0)?'Paid':'Partial'); smartBooking({paid:e.target.value}); }}/><input readOnly value={'Unpaid: '+money(Math.max(0,Number(booking.total||0)-Number(booking.paid||0)))}/></div><p className="muted small">Booked dates are removed from the Check-in options. Guests usually pay before check-in, so Paid is the default.</p></>}<Button disabled={!!bookingConflict} onClick={addBooking}>Save Booking</Button></Card><Section title="Add repair" icon={<Wrench/>}/><Card><select value={repair.room_id} onChange={e=>setRepair({...repair,room_id:e.target.value})}><option value="">Select room</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><textarea placeholder="Repair issue" value={repair.issue} onChange={e=>setRepair({...repair,issue:e.target.value})}/><Button onClick={addRepair}>Add Repair</Button></Card></div>}
    {active==='cleaning'&&<><Section title="Cleaning tasks" icon={<Brush/>}/>{tasks.map(t=><Card key={t.id}><div className="row"><div><b>{t.room_name}</b><p>{t.task_type}</p><p className="muted">{formatDate(t.task_date)} · {t.assignee}</p>{canFinance&&<p className="muted">Cost: {money(t.cost)}</p>}</div><Button kind={t.status==='Done'?'light':'primary'} onClick={()=>toggleTask(t.id,t.status)}>{t.status==='Done'?'Done':'Mark Done'}</Button></div></Card>)}</>}
    {active==='reports'&&<><Section title="Reports & backup" icon={<BarChart3/>}/><Card><input type="month" value={selectedMonth} onChange={e=>setSelectedMonth(e.target.value)}/><div className="actions"><Button onClick={exportMonthlyCSV}><Download size={16}/> Monthly CSV</Button><Button kind="light" onClick={exportBookingsCSV}><Download size={16}/> Bookings CSV</Button><Button kind="light" onClick={saveBackup}><Download size={16}/> Full Backup JSON</Button></div><p className="muted small">Backup is stored in Supabase and downloaded to your device.</p></Card><OccupancyDashboard rows={monthly} title={monthDashboardTitle(selectedMonth)}/>{monthly.map(r=><Card key={r.room_id}><div className="row"><div><b>{r.room}</b><p>Occupied nights: {r.occupied} / {r.totalDays}</p>{canFinance&&<p>Monthly income: {money(r.income)}</p>}</div><span className="pill">{r.occupancy}%</span></div><div className="bar"><i style={{width:`${Math.min(100,r.occupancy)}%`}}/></div></Card>)}</>}
    {active==='finance'&&<>{canFinance?<><Section title={`${monthShort(selectedMonth)} Finance Summary`} icon={<Wallet/>}/><Card><input type="month" value={selectedMonth} onChange={e=>setSelectedMonth(e.target.value)}/><h3 className="card-title">{formatMonthYear(selectedMonth)} Operations</h3><div className="grid mini"><Stat title={`${monthShort(selectedMonth)} Received`} value={money(financeSummary.monthReceived)}/><Stat title={`${financeSummary.year} Total Received`} value={money(financeSummary.yearReceived)}/><Stat title="Net Income" value={money(financeSummary.net)}/><Stat title="Occupancy" value={financeSummary.occ+'%'}/><Stat title="Room Nights Sold" value={`${financeSummary.soldNights}/${financeSummary.availableNights}`}/><Stat title="Avg Unit Price" value={money(financeSummary.adr)}/><Stat title="Expense Ratio" value={financeSummary.expenseRatio+'%'}/><Stat title="Unpaid" value={money(stats.unpaid)}/></div></Card><Card><h3 className="card-title">Monthly Expenses</h3><FinanceRow label="Daily Supplies" value={financeSummary.daily}/><FinanceRow label="Cleaning" value={financeSummary.cleaning}/><FinanceRow label="Repairs" value={financeSummary.repairCost}/></Card><Card><h3 className="card-title">Year {financeSummary.year} Summary</h3><FinanceRow label="Year Total Received" value={financeSummary.yearReceived} strong/><FinanceRow label="Daily Supplies" value={financeSummary.yDaily}/><FinanceRow label="Cleaning" value={financeSummary.yCleaning}/><FinanceRow label="Repairs" value={financeSummary.yRepair}/><FinanceRow label="Year Net Income" value={financeSummary.yNet} strong/></Card><Section title="Add expense" icon={<Plus/>}/><Card><select value={expense.category} onChange={e=>setExpense({...expense,category:e.target.value})}><option>Daily Supplies</option><option>Cleaning</option><option>Repairs</option><option>Other</option></select><input placeholder="Amount" value={expense.amount} onChange={e=>setExpense({...expense,amount:e.target.value})}/><input type="date" value={expense.expense_date} onChange={e=>setExpense({...expense,expense_date:e.target.value})}/><input placeholder="Note" value={expense.note} onChange={e=>setExpense({...expense,note:e.target.value})}/><Button onClick={addExpense}>Save Expense</Button></Card><Section title="Revenue by source" icon={<BarChart3/>}/><SourceBars rows={sourceRows}/><Section title="Booking records" icon={<Plus/>}/>{bookings.map(b=><BookingCard key={b.id} b={b} canFinance={canFinance} canManage={canManage} onEdit={startEditBooking}/>)}</>:<p className="muted">Your role cannot view finance data.</p>}</>}
    {active==='rooms'&&<><Section title="Rooms" icon={<Building2/>}/>{rooms.map(r=><RoomCard key={r.id} room={r} booking={currentBookings.find(b=>b.room_id===r.id)} canFinance={canFinance} canManage={canManage} onEdit={startEditBooking}/>)}</>}
    {active==='repairs'&&<><Section title="Repair records" icon={<Wrench/>}/>{repairs.map(r=><Card key={r.id}><div className="row"><div><b>{r.room_name||'General'}</b><p>{r.issue}</p><p className="muted">{new Date(r.created_at).toLocaleString()}</p></div><Button kind="light" onClick={()=>solveRepair(r.id)}>{r.status}</Button></div></Card>)}</>}
    {active==='users'&&<><Section title="Users & permissions" icon={<Shield/>}/>{canUsers?profiles.map(p=><Card key={p.id}><div className="row"><div><b>{p.full_name||p.email}</b><p className="muted">{p.email}</p></div><select value={p.role} onChange={e=>updateRole(p.id,e.target.value)}>{roles.map(r=><option key={r}>{r}</option>)}</select></div></Card>):<p className="muted">Only Boss can manage user roles.</p>}</>}
  </main><nav>{tabs.map(t=>{ const I=t.icon; if(t.id==='users'&&!canUsers)return null; return <button key={t.id} onClick={()=>setActive(t.id)} className={active===t.id?'active':''}><I size={18}/><span>{t.label}</span></button>})}</nav></div>;
}

function RoomCard({room,booking,canFinance,canManage,onEdit}){ const q=booking?nights(booking):0; const unit=booking?unitPriceFrom(booking.total,booking.check_in,booking.check_out):0; const unpaid=booking?Math.max(0,Number(booking.total||0)-Number(booking.paid||0)):0; return <Card><div className="row"><div><b>{room.name}</b><p className="muted">{booking?.guest||'No current guest'}</p>{booking?.guest_phone&&<a className="whatsapp" href={whatsappUrl(booking.guest_phone)} target="_blank"><Phone size={14}/> WhatsApp</a>}</div><span className={booking?'pill dark':'pill'}>{booking?'Occupied':'Available'}</span></div>{booking?<><p>Check-in: {formatDate(booking.check_in)}</p><p>Check-out: {formatDate(booking.check_out)}</p><p>Qty: {q} nights</p>{canFinance&&<><p>Unit price: {money(unit)} / night</p><p>Total: {money(booking.total)} · Paid: {money(booking.paid)}</p><p>Unpaid: {money(unpaid)}</p></>}</>:<p className="muted">Available for new booking</p>}{booking&&canManage&&<div className="actions"><Button kind="light" onClick={()=>onEdit?.(booking)}><Edit3 size={16}/> Edit Booking</Button></div>}</Card> }
function BookingCard({b,canFinance,canManage,onEdit}){ const q=nights(b); const unit=unitPriceFrom(b.total,b.check_in,b.check_out); const unpaid=Math.max(0,Number(b.total||0)-Number(b.paid||0)); return <Card><div className="row"><div><b>{b.room_name}</b><p>{b.guest} · {b.source}</p>{b.guest_phone&&<a className="whatsapp" href={whatsappUrl(b.guest_phone)} target="_blank"><Phone size={14}/> WhatsApp</a>}</div><span className="pill">{q} nights</span></div><p>{formatDate(b.check_in)} → {formatDate(b.check_out)}</p>{canFinance&&<><p>Unit price: {money(unit)} / night</p><p>Paid: {money(b.paid)} / Total: {money(b.total)} · Unpaid: {money(unpaid)}</p></>}{canManage&&<div className="actions"><Button kind="light" onClick={()=>onEdit?.(b)}><Edit3 size={16}/> Edit</Button></div>}</Card> }
function FinanceRow({label,value,strong=false}){ return <div className="row finance-row"><span className={strong?'strong':''}>{label}</span><b>{money(value)}</b></div>; }
function OccupancyDashboard({rows,title}){ const avg=rows.length?Math.round(rows.reduce((s,r)=>s+r.occupancy,0)/rows.length):0; const best=[...rows].sort((a,b)=>b.occupancy-a.occupancy)[0]; return <Card><h3 className="card-title">{title || 'Monthly Occupancy Dashboard'}</h3><p className="muted small">Occupancy percentage is calculated for the selected month only.</p><div className="grid mini"><Stat title="Average Monthly Occupancy" value={avg+'%'}/><Stat title="Best Room" value={best?.room||'-'}/></div>{rows.map(r=><div className="bar-row" key={r.room}><span>{r.room}</span><div className="bar"><i style={{width:`${Math.min(100,r.occupancy)}%`}}/></div><b>{r.occupancy}%</b></div>)}</Card> }
function LineChart({rows}){ const max=Math.max(1,...rows.map(r=>r.revenue)); const points=rows.map((r,i)=>`${(i/(rows.length-1))*100},${100-(r.revenue/max)*85}`).join(' '); return <Card><svg viewBox="0 0 100 110" className="chart"><polyline points={points} fill="none" stroke="currentColor" strokeWidth="3"/><line x1="0" y1="100" x2="100" y2="100" stroke="currentColor" strokeOpacity=".15"/></svg><div className="trend-labels">{rows.map(r=><span key={r.month}>{r.month.slice(5)}<br/>{money(r.revenue)}</span>)}</div></Card> }
function SourceBars({rows}){ if(!rows.length)return <Card><p className="muted">No revenue data yet.</p></Card>; const max=Math.max(...rows.map(r=>r.revenue)); return <Card>{rows.map(r=><div className="bar-row" key={r.source}><span>{r.source}</span><div className="bar"><i style={{width:`${(r.revenue/max)*100}%`}}/></div><b>{money(r.revenue)}</b></div>)}</Card> }

createRoot(document.getElementById('root')).render(<App/>);